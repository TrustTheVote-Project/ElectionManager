<h1>New district_type</h1>

<% form_for(@district_type) do |f| %>
  <%= render_error_messages(@district_type) %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', district_types_path %>