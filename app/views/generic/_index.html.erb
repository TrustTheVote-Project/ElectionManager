<div class="block">
  <div class="content">
    <h2 class="title"><%= t("ttv.all", :default => "All") + " " + title_of %></h2>
    <%= ttv_view_list(collection, columns, commands) %>
  </div>
</div>
<% content_for :sidebar, render(:partial => 'sidebar') -%>