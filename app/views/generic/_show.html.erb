<div class="block">
    <div class="content">
        <div class="inner">
          <% fields.each do |fld| %>
            <%= ttv_show_field(record, fld) %>
          <% end %>
          <%= ttv_actions_bar(record, [:edit, :delete, :back])%>
        </div>
    </div>
</div>
<% content_for :sidebar, render(:partial => 'sidebar') -%>